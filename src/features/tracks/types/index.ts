// Re-export types from API for convenience
export type {
  CreateTrackPayload,
  FetchTracksParams as TrackFilters,
  TrackDetailRecord as TrackDetail,
  TrackEventRecord as TrackEvent,
  TrackRecord as Track,
  UpdateTrackPayload,
} from '@/app/api/tracks';
